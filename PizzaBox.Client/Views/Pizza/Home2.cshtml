@*Strong Typed View*@

@model IEnumerable<PizzaBox.Domain.Models.PizzaModel>;


<h2>Pizza List</h2>
    <ul>
        @foreach (var p in @Model)
        {
            <li>@p</li>
        }
    </ul>

    CreatePizza

    <form action=""> PizzaViewModel
        display crust
        display sizes
        display toppings
    </form>